import React from 'react';
import { Button } from '../ui/button';

const Home = () => {
    return (
        <div className=' text-red-700'>
      HomePage
      <Button>ShadCn</Button>
    </div>
    );
};

export default Home;